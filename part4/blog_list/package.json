{
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env index.js",
    "test": "node --test --env-file=.env tests/*.test.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.17.1"
  },
  "devDependencies": {
    "eslint": "^9.33.0",
    "globals": "^16.3.0",
    "supertest": "^7.1.4"
  }
}
